.container
  .row
    .col-md-6.col-md-offset-1
      .jumbotron
        %h1 New article

        = bootstrap_form_for @article do |f|
          = f.text_field :title
          = f.text_area :body
          = f.text_field :img_url
          = f.submit "Create"